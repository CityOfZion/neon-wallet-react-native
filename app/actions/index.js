import * as walletActions from './wallet'

export const ActionCreators = Object.assign({}, { wallet: walletActions })

export const ActionConstants = Object.assign({}, { wallet: walletActions.constants })
